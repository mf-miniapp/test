## 企业级存储方案分析

### 数据特征

| 特征 | 分析 |
|------|------|
| 数据模型 | 严格树形结构 (ROOT → SUB_DOMAIN → IP → PORT → SERVICE → ENDPOINT) |
| 访问模式 | 读多写少（查询/遍历 > 创建/更新） |
| 并发需求 | 多 worker 并发探测写入 + Web UI 并发读取 |
| 数据量 | 中等（单任务可能 10K-100K 节点） |
| 一致性 | 强一致性（状态变更需持久化） |
| 查询复杂度 | 中等（路径查询、状态过滤、统计聚合） |

---

### 推荐方案：PostgreSQL

**为什么选 PostgreSQL 而非 MySQL/MongoDB：**

| 考量 | PostgreSQL | MySQL | MongoDB |
|------|-----------|-------|---------|
| 树形查询 | ✅ CTE 递归查询原生支持 | ⚠️ 需要额外实现 | ⚠️ 聚合管道复杂 |
| JSON 支持 | ✅ JSONB 二进制存储 + 索引 | ⚠️ JSON 类型限制 | ✅ 原生文档 |
| 并发控制 | ✅ MVCC 无锁读 | ⚠️ 锁粒度较粗 | ✅ 文档级锁 |
| 扩展性 | ✅ 分区表、物化视图 | ⚠️ 分区支持有限 | ✅ 原生分片 |
| 审计日志 | ✅ pgAudit / 触发器 | ⚠️ 需要额外配置 | ⚠️ Change Streams 有限 |
| 全文搜索 | ✅ 内置 tsvector | ⚠️ 独立全文索引 | ✅ 内置但功能有限 |

---

### 数据库架构设计

```sql
-- 1. 主表：树元数据
CREATE TABLE asset_trees (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name VARCHAR(255) NOT NULL,
    root_domain VARCHAR(255) NOT NULL,
    description TEXT,
    created_by VARCHAR(100),
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW(),
    is_deleted BOOLEAN DEFAULT FALSE  -- 软删除
);

-- 2. 核心表：节点 (支持树形查询)
CREATE TABLE asset_nodes (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    tree_id UUID NOT NULL REFERENCES asset_trees(id) ON DELETE CASCADE,
    parent_id UUID REFERENCES asset_nodes(id) ON DELETE SET NULL,
    
    -- 节点标识
    node_id VARCHAR(100) NOT NULL,  -- 树内唯一 ID
    node_type VARCHAR(50) NOT NULL,  -- 'root_domain', 'sub_domain', etc.
    
    -- 核心属性
    title VARCHAR(500),
    state VARCHAR(50) DEFAULT 'unseen',  -- 枚举状态
    url TEXT,
    asset_type VARCHAR(50),  -- 'webapp', 'server', 'db'
    ip_address INET,  -- 原生 IP 类型
    
    -- 元数据 (JSONB 支持灵活扩展)
    meta JSONB DEFAULT '{}',
    
    -- 时间戳
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW(),
    
    -- 索引约束
    UNIQUE(tree_id, node_id),
    UNIQUE(tree_id, parent_id, node_id)
);

-- 3. 索引优化
CREATE INDEX idx_nodes_tree ON asset_nodes(tree_id);
CREATE INDEX idx_nodes_parent ON asset_nodes(parent_id);
CREATE INDEX idx_nodes_type ON asset_nodes(tree_id, node_type);
CREATE INDEX idx_nodes_state ON asset_nodes(tree_id, state);
CREATE INDEX idx_nodes_meta ON asset_nodes USING GIN (meta);  -- JSONB 索引
CREATE INDEX idx_nodes_ip ON asset_nodes(ip_address);

-- 4. 审计日志表
CREATE TABLE asset_audit_log (
    id BIGSERIAL PRIMARY KEY,
    tree_id UUID REFERENCES asset_trees(id),
    node_id UUID REFERENCES asset_nodes(id),
    action VARCHAR(50) NOT NULL,  -- 'create', 'update', 'delete', 'state_change'
    old_value JSONB,
    new_value JSONB,
    changed_by VARCHAR(100),
    changed_at TIMESTAMPTZ DEFAULT NOW()
);

-- 5. 物化视图：统计汇总 (定期刷新)
CREATE MATERIALIZED VIEW asset_tree_stats AS
SELECT 
    tree_id,
    COUNT(*) as total_nodes,
    COUNT(DISTINCT state) as state_variety,
    COUNT(*) FILTER (WHERE state = 'discovered') as discovered_count,
    COUNT(*) FILTER (WHERE state = 'exploited') as exploited_count,
    COUNT(*) FILTER (WHERE state = 'abandoned') as abandoned_count,
    MAX(updated_at) as last_activity
FROM asset_nodes
GROUP BY tree_id;

CREATE UNIQUE INDEX idx_stats_tree ON asset_tree_stats(tree_id);
```

---

### 核心查询示例

```sql
-- 1. 递归查询：获取整棵树
WITH RECURSIVE tree AS (
    SELECT * FROM asset_nodes WHERE parent_id IS NULL AND tree_id = $1
    UNION ALL
    SELECT n.* FROM asset_nodes n
    JOIN tree t ON n.parent_id = t.id
)
SELECT * FROM tree ORDER BY created_at;

-- 2. 路径查询：节点到根的路径
WITH RECURSIVE path AS (
    SELECT id, node_id, title, parent_id, ARRAY[id] as path_ids
    FROM asset_nodes WHERE id = $1
    UNION ALL
    SELECT n.id, n.node_id, n.title, n.parent_id, p.path_ids || n.id
    FROM asset_nodes n JOIN path p ON n.id = p.parent_id
)
SELECT * FROM path ORDER BY path_ids;

-- 3. 状态变更：批量更新
UPDATE asset_nodes 
SET state = 'discovered', updated_at = NOW()
WHERE tree_id = $1 AND node_id = ANY($2);

-- 4. 统计查询
SELECT 
    node_type,
    state,
    COUNT(*) as count,
    AVG(EXTRACT(EPOCH FROM (NOW() - created_at))) as avg_age_seconds
FROM asset_nodes
WHERE tree_id = $1
GROUP BY node_type, state;
```

---

### 连接池配置

```python
# config.py
from sqlalchemy import create_engine
from sqlalchemy.pool import QueuePool

DATABASE_URL = "postgresql://user:pass@host:5432/opensquilla"

engine = create_engine(
    DATABASE_URL,
    poolclass=QueuePool,
    pool_size=20,           # 常驻连接数
    max_overflow=30,        # 最大溢出连接
    pool_timeout=30,        # 获取连接超时
    pool_recycle=1800,      # 连接回收时间 (30分钟)
    pool_pre_ping=True,     # 健康检查
)
```

---

### 部署建议

| 环境 | 配置 |
|------|------|
| **开发** | Docker 本地单实例 |
| **测试** | 主从复制 (1主1从) |
| **生产** | PgBouncer + 2主1从 + 定时备份 |
| **高可用** | Patroni 自动故障转移 |

---

需要我实现 SQLAlchemy ORM 模型和迁移脚本吗？
