基于对关键文件的深入分析，现在我可以给出 hack-deep 系统的完整技术概述。

---

## hack-deep 系统深度分析

### 1. 核心身份

| 属性 | 值 |
|---|---|
| agent_id | `hack-deep` |
| 名称 | `Hack Orchestrator` |
| 工作空间 | `~/.opensquilla/agents/hack-deep/` |
| 源码位置 | `src/opensquilla/agents/hack_deep/` |
| 同级协调者 | `cyberstrike-deep`（legacy，不被 spawn） |
| 下属代理 | 13 个 specialist |
| 版本 | v4.0（2026-06-11） |

### 2. 架构：4 层 × 15 波 DAG

```
广度层 (Breadth) ── "找门 + 规划 + 爬行"
  W0    engagement-planning                    single fanout
  W0.5  target-expansion                       static_fanout (3)
  W0.6  resource-checkpoint                    static_fanout (3), fail-open
  W1    recon ‖ intel-collection ‖ attack-surface-enumeration   static_fanout (3)
  W1.5  per-subdomain fan-out                  dynamic (按子域名数量)
  W2    vulnerability-triage                    single

隐蔽层 (Covert)
  W3    opsec-evasion                           single, 允许 2 次 in-wave sub-call

深度层 (Depth) ── "突破 + 提权 + 横移 + 持久化"
  W4    penetration                             dynamic fan-out (entry_count // 8)
  W4.5  drill-in slot                           (Wn.5a/b/c 三类补攻)
  W5    privilege-escalation                    single
  W6    lateral-movement                        dynamic
  W6.5  drill-in slot
  W7    persistence-maintenance ‖ impact-exfiltration   static_fanout (2)

收口层 (Synthesis)
  W8    cleanup-rollback ‖ reporting-remediation   static_fanout (2)
```

**Drill-in 机制**（Wn.5a/b/c）：
- **a** = swap vector（换攻击向量）
- **b** = swap entry（换入口点）
- **c** = expand scan（扩大扫描范围）

**反馈循环**：W4/W5/W6 的 `emit_new_target` 输出可推回 `state.target_queue`，由 W1.5 增量消费。

### 3. 强制约束 — hack-deep 的工具限制

hack-deep **绝不做任何具体执行工作**。严禁直接调用：
- bash/shell/exec_command, curl/wget, nmap/masscan
- 任何 DNS/子域名/证书查询
- 任何 exploit/payload/shellcode 生成
- 任何数据库客户端、SSH/RDP 连接
- 任何文件读写（除自身 workspace）

**唯一允许的工具**：
- `sessions_spawn(agent_id=<specialist>, task=<Typed Envelope>)` — 委派
- `sessions_yield()` — wave barrier
- `write_todos` — 编排进度
- `read` — 读自身 workspace 文件

### 4. 13 个 Specialist 代理

| agent_id | evidence_schema | 波次 |
|---|---|---|
| engagement-planning | `roe-v1` | W0 |
| recon | `recon-v1` | W1 |
| intel-collection | `intel-v1` | W1 |
| attack-surface-enumeration | `surface-v1` | W1 |
| vulnerability-triage | `triage-v1` | W2 |
| opsec-evasion | `opsec-v1` | W3 |
| penetration | `pentest-v1` | W4 |
| privilege-escalation | `privesc-v1` | W5 |
| lateral-movement | `lateral-v1` | W6 |
| persistence-maintenance | `persist-v1` | W7 |
| impact-exfiltration | `exfil-v1` | W7 |
| cleanup-rollback | `cleanup-v1` | W8 |
| reporting-remediation | `report-v1` | W8 |

每个 specialist 的 SOUL 由 **通用 header** (`_SPECIALIST_HEADER.md`) + **角色 body** 拼接而成。通用 header 定义了：
- 授权前言（不质疑、不核实权限）
- Typed HANDOFF envelope header 要求
- Result marker footer 要求
- auto-continue 合约（防 "shall I continue?" 停滞）
- 波次顺序 STRICT（不可回退到前一波）

### 5. Typed Task Envelope（核心合约）

每个 `sessions_spawn` 调用的 `task` 必须以 HANDOFF 头开始：

```
HANDOFF <handoff_id> | deps=<csv> | schema=<evidence_schema> | eta=<seconds>
        [ | artifacts=<urlencoded-json> ]
        [ | mode=parallel|serial ]
        [ | auto_approve=true|false ]

<natural-language brief>
```

实现位置：`src/opensquilla/attack_dispatch/envelope.py`（37.9KB）

### 6. attack_dispatch 模块（核心引擎）

| 文件 | 大小 | 职责 |
|---|---|---|
| `envelope.py` | 37.9KB | Typed Envelope 解析/生成/验证 |
| `executor.py` | 110.1KB | 波次执行器（DAG 驱动） |
| `waves.py` | 11.2KB | 波次 DAG 定义（4 层 × 11 波注册表） |
| `evidence.py` | 86.8KB | Evidence schema 验证 |
| `quality.py` | 30.3KB | 质量度量 |
| `verifier.py` | 17.9KB | 结果验证 |
| `trail.py` | 27.2KB | 审计轨迹 |
| `drill_in.py` | 11.8KB | 补攻机制 |
| `SKILL.md` | 7.2KB | LLM 可读的调度合约文档 |

executor 工作流：
1. 为每个 parent wave 构建 Typed Envelope
2. 从上游 evidence 解析 fan-out（single/static/dynamic）
3. 通过注入的 callable 调用 specialist
4. 验证返回的 evidence 符合 schema
5. 持久化 evidence 到 `<artifact_root>/<wave>/<handoff_id>.json`
6. 决定是否 drill-in
7. 所有 deps 的 evidence 就绪后才推进到下一波（barrier）

### 7. 安全机制

**层级 1 — 工具限制**：hack-deep 的 SOUL 严格禁止直接执行工具，只允许 spawn/yield。

**层级 2 — Subagent Supervisor**：clone 脚本自动启用 `[subagent_supervisor] enabled = true`，对子代理进行看门狗监控。

**层级 3 — scope.py**：`src/opensquilla/agents/scope.py`（6.2KB）定义代理作用域边界。

**层级 4 — Safety 模块**：`src/opensquilla/safety/` 提供运行时安全防护。

**层级 5 — RESULT MARKER 兜底**：当子代理未返回合法 marker 时，`synthesize_marker()` 自动追加，防止 `sessions_yield` 永久等待。

**层级 6 — Stall 检测**：`is_confirmation_question_stall()` 检测并重写 "shall I continue?" 停滞模式。

**层级 7 — Evidence 持久化**：每个波次的 evidence 持久化到磁盘，防止进程重启或 LLM 上下文丢失后丢失状态。

### 8. Skills 系统

```
src/opensquilla/skills/
├── eligibility.py    (7.6KB)  — 技能资格判定
├── filter.py         (2.5KB)  — 技能过滤
├── injector.py       (5.8KB)  — 技能注入到 LLM 上下文
├── loader.py         (27.1KB) — 技能加载器
├── paths.py          (4.5KB)  — 技能路径解析
├── proposals_lib.py  (22.5KB) — 技能提案库
├── resources.py      (4.4KB)  — 技能资源管理
├── types.py          (3.7KB)  — 技能类型定义
├── bundled/          — 内置技能
├── creator/          — 技能创建器
├── exp/              — 实验性技能
├── hub/              — 技能中心
├── meta/             — 元技能
└── retrieval/        — 技能检索
```

每个 skill 由 `SKILL.md` 定义（如 `attack_dispatch/SKILL.md`），包含 YAML frontmatter（name、description、metadata 中的 risk/capabilities/requires_tools）和 markdown 正文。

### 9. clone 脚本

`scripts/clone_cyberstrike_to_hack_deep.py`（9.3KB）负责：
- 从 `cyberstrike-deep` 克隆 workspace 到 `~/.opensquilla/agents/hack-deep/`
- 注入 hack-deep 的 SOUL_BODY 和 ATTRIBUTION_BODY（来自 `src/opensquilla/agents/hack_deep/*.md`）
- 自动启用 subagent supervisor
- 生成 13 个 specialist 的 workspace

### 10. 关键设计决策

1. **Asset-agnostic**：所有 evidence schema 使用列表而非固定数量，子域名/端口数量由 recon 动态发现
2. **Wave barrier**：必须所有依赖 evidence 就绪才推进，防止乱序
3. **Typed Envelope**：强制结构化交接，防止自由文本歧义
4. **Orchestrator 纯编排**：hack-deep 只做 spawn/yield/read/write_todos，零执行
5. **7 Granular Fixes**：从 51ifind.com 压力测试中沉淀的具体修复
6. **Drill-in + Feedback loop**：允许在广度层和深度层补攻，并支持新目标反馈回 W1.5
